# Core CLI tools
fzf
ripgrep
git
zsh
tmux

# Node.js via FNM (installed by script)

# Neovim — installed via install_neovim_darwin() (latest stable tarball)
# Do NOT add neovim here; brew's version lags behind releases

# Tools
age
stow

# Starship prompt (installed by script)
